name: Preview Web App (develop + feature branches + PRs)

on:
  push:
    branches:
      - develop
      - 'feature/**'
    paths:
      - 'apps/web/**'
      - 'packages/**'
      - 'Dockerfile.web'
      - 'pnpm-lock.yaml'
      - '.github/workflows/preview-web.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'apps/web/**'
      - 'packages/**'
      - 'Dockerfile.web'
      - 'pnpm-lock.yaml'

concurrency:
  group: preview-web-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy-preview:
    name: Build & Deploy Preview
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      packages: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push preview image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.web
          push: true
          tags: |
            ghcr.io/silent-suite/silentsuite-web:preview
            ghcr.io/silent-suite/silentsuite-web:preview-${{ github.sha }}
          build-args: |
            NEXT_PUBLIC_ETEBASE_SERVER_URL=https://server.silentsuite.io
            NEXT_PUBLIC_BILLING_API_URL=https://api.silentsuite.io
            NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=${{ secrets.STRIPE_PUBLISHABLE_KEY }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy preview to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            # Pull the new preview image
            docker pull ghcr.io/silent-suite/silentsuite-web:preview

            # Stop existing preview container
            docker stop silentsuite-web-preview 2>/dev/null || true
            docker rm silentsuite-web-preview 2>/dev/null || true

            # Start new preview container on port 3001
            docker run -d \
              --name silentsuite-web-preview \
              --restart unless-stopped \
              -p 127.0.0.1:3001:3000 \
              -e NODE_ENV=production \
              ghcr.io/silent-suite/silentsuite-web:preview

            echo "Preview deployed to https://previewapp.silentsuite.io"

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const body = `**Preview deployed** ✅\n\n🔗 https://previewapp.silentsuite.io\n\nCommit: \`${context.sha.substring(0, 7)}\`\nImage: \`ghcr.io/silent-suite/silentsuite-web:preview-${context.sha.substring(0, 7)}\``;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number
            });

            const existing = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes('Preview deployed')
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body
              });
            }
